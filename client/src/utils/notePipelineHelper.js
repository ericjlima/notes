export const retrievePaths = (name, namepid) => {
  const paths = [];
  //TODO: finish this function


  return paths;
};


  //TODO: perhaps if the above is causing trouble or too much confusion, just save a 'path' column in your notes for each note and use that instead of namepid. Rplace namepid entirely as it's not effecient and just too confusing. However, this may be just as confusing if not more.
